'use server';

export async function submitContactForm(data: any) {
  console.log("دیتای دریافتی در سرور:", data);
  
  // ۱. اینجا دیتای ورودی را با اسکمای contactSchema که در کامپوننت داری چک کن
  
  // ۲. با دستور prisma.contactMessage.create دیتا را در PostgreSQL بنویس
  
  // ۳. با پکیج resend ایمیل را ارسال کن
  
  // ۴. در نهایت یک وضعیت موفقیت یا خطا برگردان: return { success: true }
    
}